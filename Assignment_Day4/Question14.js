//Write a program to return reverse of an array
function reverseArray(arr) {
    return arr.reverse();
}
const inputArray = [1, 2, 3, 4, 5];
const reversedArray = reverseArray(inputArray);
console.log(reversedArray);

